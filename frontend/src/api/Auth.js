/* eslint-disable no-unused-vars */
// import { getApiCall, postApiCall } from '../utils/Api';

export const loginUser = async (data) => {
  // const result = await postApiCall('/login', data);
  // return result.data;
};

export const registerUser = async (data) => {
  // const result = await postApiCall('/signup', data);
  // return result.data;
};

export const validateSession = async () => {
  // const result = await getApiCall('/validate-session');
  // return result.data;
};

export const logoutUser = async () => {
  // const result = await getApiCall('/logout');
  // return result.data;
};
